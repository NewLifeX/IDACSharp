++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
2011-04-20  by dreamzgj
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
命名:
# 对于文件的命名,如dbg.hpp,封将为文件Dbg.h

# 对于每一个头文件的封装命名，如dbg.hpp封装为类 Ida_Dbg

# 对IDA sdk 函数命名，在原名称的基础上采用的首字母大写,例run_requests(void),封装为
  Run_Requests(void)
  
# 结构体命名,如sdk内部断点结构bpt_t,封装为Struct_Bpt_T

# 宏定义的封装,对于每组宏定义封装为enum类型,如对于断点类型bpttype_t,封装为Enum_BptType_T

更新记录:
# 增加了Dbg.h头文件,对调试Api进行了封装，有个别api暂未进行封装

# 增加了Idd.h头文件,封装了调试Api所需要用到的一些结构

# 增加了Loader.h头文件,其中定义的Hook安装以及Hook的卸载还有Hook到消息后的回调
  是控件调试器所必须的,没有封装成功
  
# 在kernelWin.h中增加了enum class Ui_Notification_T的定义

# 重命名IDA.h为IdaHelper.h,并增加了数组托管与非托管的转换方法

# 相应的对inc.h进行更新

注:
# 封装出来的文件开头都注明了封装记录


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
2010-05-08 08:58  by 大石头
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
1，原则上，尽可能进行面向对象封装，各方法尽可能封装为成员方法
2，必须实现执行IDC脚本的方法，在难以封装的函数中可以调用IDC脚本来处理